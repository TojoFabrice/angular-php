Techno utilisées:
* PHP 7.4.0
* mysql 
* wamp server 3.2.2.2
* Angular 11
* nodejs

j'ai utilisé visual code pour l'editeur

avant le lancement du projet angular (front), faire "npm install" pour installer tous les package utilisé.
Créer une base de donnée nommé "ingenosiadb"
Puis importer la table "utilisateur.sql" dans la BDD. 
le fichier sql se situe dans php_REST>config

-> cmd pour lancer le back: php -S 127.0.0.1:8080
-> cmd pour lancer le gront: ng serve -o
